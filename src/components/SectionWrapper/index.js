import React from 'react'

import { SectionWrapperStyled } from './styles'

const SectionWrapper = ({children}) => (
  <SectionWrapperStyled>{children}</SectionWrapperStyled>
)

export default SectionWrapper